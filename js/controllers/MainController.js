app.controller('MainController',  function($scope) {
    $scope.title = 'Cards of HearthStone!';
});

app.controller('AppController',  function($scope) {
    $scope.title = 'Cards of HearthStone!';
});